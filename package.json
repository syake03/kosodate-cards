{
  "name": "kosodate-cards",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node scripts/check-sources.mjs && astro build && pagefind --site dist",
    "build:nosearch": "node scripts/check-sources.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.7.5",
    "marked": "^15.0.7"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
